---
sidebar_label: "Docker"
---

import Quiz from '@site/src/components/Quiz';

# Docker - Práctica Interactiva

## Quiz

Poné a prueba tus conocimientos sobre Docker.

<Quiz preguntas={[
  {
    pregunta: "¿Qué es Docker y qué problema resuelve?",
    opciones: [
      "Un lenguaje de programación para crear aplicaciones web",
      "Una plataforma que permite desarrollar, empaquetar y ejecutar aplicaciones en contenedores, garantizando portabilidad y aislamiento",
      "Un sistema operativo alternativo a Linux",
      "Un servicio de hosting en la nube para desplegar servidores"
    ],
    respuesta: 1,
    explicacion: "Docker es una plataforma que permite desarrollar, empaquetar y ejecutar aplicaciones en contenedores. Un contenedor es una unidad ligera y portátil que contiene todo lo necesario para ejecutar una aplicación: código, runtime, librerías y configuraciones del sistema. Resuelve problemas de portabilidad, aislamiento y reproducibilidad entre entornos."
  },
  {
    pregunta: "¿Cuál es la diferencia entre una imagen y un contenedor en Docker?",
    opciones: [
      "No hay diferencia, son sinónimos",
      "La imagen es el contenedor en ejecución y el contenedor es la plantilla estática",
      "La imagen es una plantilla inmutable (template) y el contenedor es una instancia en ejecución de esa imagen",
      "La imagen se usa solo en producción y el contenedor solo en desarrollo"
    ],
    respuesta: 2,
    explicacion: "Una imagen es la definición estática (template) de lo que contendrá el contenedor. Es inmutable y contiene todo el entorno necesario (SO, librerías, binarios). Un contenedor es una instancia en ejecución de esa imagen, con su propio sistema de archivos, red y procesos."
  },
  {
    pregunta: "¿Qué hace el comando `docker run`?",
    opciones: [
      "Construye una imagen a partir de un Dockerfile",
      "Muestra los contenedores activos en el sistema",
      "Crea y ejecuta un nuevo contenedor a partir de una imagen",
      "Elimina todos los contenedores detenidos"
    ],
    respuesta: 2,
    explicacion: "El comando `docker run` crea una nueva instancia de un contenedor a partir de una imagen y lo ejecuta. Si la imagen no está disponible localmente, Docker la descarga desde Docker Hub antes de crear el contenedor."
  },
  {
    pregunta: "¿Qué es un Dockerfile?",
    opciones: [
      "Un archivo binario que Docker usa internamente para gestionar redes",
      "Un archivo de texto con instrucciones declarativas para construir una imagen Docker",
      "Un archivo de configuración exclusivo de Docker Compose",
      "Un log que Docker genera automáticamente al ejecutar contenedores"
    ],
    respuesta: 1,
    explicacion: "Un Dockerfile es un archivo de texto que contiene una serie de instrucciones declarativas para construir una imagen Docker. En él se especifica el sistema base, las dependencias, el código a copiar, variables de entorno y el comando que se ejecutará al iniciar el contenedor."
  },
  {
    pregunta: "¿Qué hace la instrucción `FROM` en un Dockerfile?",
    opciones: [
      "Copia archivos desde el host al contenedor",
      "Define el comando que se ejecuta al iniciar el contenedor",
      "Especifica la imagen base sobre la cual se construirá la nueva imagen",
      "Expone un puerto del contenedor al exterior"
    ],
    respuesta: 2,
    explicacion: "La instrucción `FROM` define la imagen base a partir de la cual se construye la nueva imagen. Por ejemplo, `FROM node:14` indica que se parte de una imagen con Node.js versión 14 ya instalado. Es generalmente la primera instrucción de un Dockerfile."
  },
  {
    pregunta: "¿Qué hace el flag `-p 8080:80` al ejecutar un contenedor con `docker run`?",
    opciones: [
      "Asigna 8080 MB de memoria y 80 MB de disco al contenedor",
      "Mapea el puerto 8080 del host al puerto 80 del contenedor",
      "Mapea el puerto 80 del host al puerto 8080 del contenedor",
      "Abre los puertos 8080 y 80 en el firewall del sistema"
    ],
    respuesta: 1,
    explicacion: "El flag `-p 8080:80` realiza un mapeo de puertos (port mapping). El formato es `-p <puerto_host>:<puerto_contenedor>`. En este caso, el tráfico que llegue al puerto 8080 de la máquina host se redirige al puerto 80 dentro del contenedor."
  },
  {
    pregunta: "¿Qué es Docker Compose?",
    opciones: [
      "Una interfaz gráfica para administrar contenedores Docker",
      "Un lenguaje de programación para escribir Dockerfiles",
      "Una herramienta que permite definir y ejecutar múltiples contenedores como una única aplicación usando un archivo YAML",
      "Un reemplazo de Docker Engine para sistemas Windows"
    ],
    respuesta: 2,
    explicacion: "Docker Compose es una herramienta que permite definir, configurar y ejecutar múltiples contenedores Docker como una única aplicación. Todo se gestiona desde un archivo YAML (docker-compose.yml), donde se describen los servicios, volúmenes, redes y variables de entorno de la aplicación."
  },
  {
    pregunta: "¿Qué muestra el comando `docker ps`?",
    opciones: [
      "Todas las imágenes disponibles en Docker Hub",
      "Los contenedores que están actualmente en ejecución",
      "El historial de comandos ejecutados en Docker",
      "Los volúmenes montados en el sistema"
    ],
    respuesta: 1,
    explicacion: "El comando `docker ps` lista los contenedores que se están ejecutando actualmente, mostrando información como el ID del contenedor, la imagen utilizada, el comando en ejecución, los puertos mapeados y el estado. Para ver también los contenedores detenidos se usa `docker ps -a`."
  },
  {
    pregunta: "¿Qué hace el flag `-v` (volumen) al ejecutar un contenedor?",
    opciones: [
      "Activa el modo verbose para ver logs detallados",
      "Verifica que la imagen esté actualizada antes de ejecutar",
      "Monta un directorio del host o un volumen de Docker dentro del contenedor para persistir datos",
      "Establece la versión de Docker a utilizar"
    ],
    respuesta: 2,
    explicacion: "El flag `-v` permite montar volúmenes o bind mounts en un contenedor. Los volúmenes son gestionados por Docker y sirven para persistir datos más allá del ciclo de vida del contenedor. Los bind mounts enlazan carpetas del host con el contenedor, lo cual es útil en desarrollo para reflejar cambios en tiempo real."
  },
  {
    pregunta: "¿Qué hace la instrucción `EXPOSE` en un Dockerfile?",
    opciones: [
      "Abre automáticamente el puerto en el firewall del host",
      "Publica el puerto para que sea accesible desde fuera del contenedor sin necesidad de `-p`",
      "Documenta qué puerto usa la aplicación dentro del contenedor, pero no lo publica automáticamente",
      "Bloquea el acceso externo al puerto especificado por seguridad"
    ],
    respuesta: 2,
    explicacion: "La instrucción `EXPOSE` en un Dockerfile sirve como documentación, indicando qué puertos usa la aplicación dentro del contenedor. Sin embargo, no publica el puerto automáticamente hacia el host. Para que el puerto sea accesible desde fuera, es necesario usar el flag `-p` al ejecutar el contenedor con `docker run`."
  }
]} />
